// Declare a constant variable named 'books' and initialize it with an empty array.
const books = [];

// Export the 'books' array as a module, making it accessible to other parts of the codebase.
module.exports = books;
